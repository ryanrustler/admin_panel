<h1> Edit Courses </h1>

<%= form_with model: @course, url: course_path, method: :patch do |f| %>
<h2><%= f.label :name %>:</h2>
<%= f.text_field :name %><br> 

<h2><%= f.label :hours %>:</h2>
<%= f.text_field :hours %><br>

<h2><%= f.label :create_course%></h2>
<%= f.submit %>

<% end %>